// Coastal Healthcare Advocates — inline consultation form in the #intake section.
// Same validation / Netlify submit as the lightbox, via the shared module.

import { initContactForm } from './contact-form.js';

initContactForm(document.getElementById('intake-form'));
